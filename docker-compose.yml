version: '2'

services:
  web:
    image: user/web
    build:
      context: ./src/Web
      dockerfile: Dockerfile
      args:
        source: ${DOCKER_BUILD_SOURCE}

  api:
    image: user/api
    build:
      context: ./src/Api
      dockerfile: Dockerfile
      args:
        source: ${DOCKER_BUILD_SOURCE}

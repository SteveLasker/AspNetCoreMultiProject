version: '2'

services:
  web:
    image: dockertools.azurecr.io/aspnetcoremultiproject/web${TAG}
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80"
  api:
    image: dockertools.azurecr.io/aspnetcoremultiproject/api
